import type { ConversationContextType } from "@/lib/types";

export const CONTEXT_TYPE_LABELS: Record<ConversationContextType, string> = {
  announcement: "Annonce",
  initiative: "Initiative",
  event: "Événement",
};
